type: tali

status:
  class: exporter
  stability:
    development: [traces, metrics, logs]
  distributions: [contrib]
  codeowners:
    active: [jakobgt]

tests:
  config:
    s3_dev_null_mode: true
